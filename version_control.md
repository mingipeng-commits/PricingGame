# Pricing Game - Version Control

## Version Numbering Rules

Format: **ver.X.Y.Z**

| Segment | Rule | Example |
|---------|------|---------|
| **X** (Major) | Major changes — new rounds, architectural redesign, breaking changes | ver.2.0.0 |
| **Y** (Minor) | Minor changes — new simple functions and features | ver.1.3.0 |
| **Z** (Patch) | Bug fixes or cosmetic changes | ver.1.2.1 |

---

## Changelog

### ver.1.6.0 — 2026-04-03
**手機掃碼提交願付價格（WTP）**
- 同學可掃描 QR Code 在手機上提交願付價格（姓名 + 金額），取代便利貼
- 手機端自動偵測當前階段：WTP 收集時顯示姓名/價格輸入；定價階段顯示組別選擇
- 老師端即時顯示手機已提交的願付價格（綠色標籤，含姓名與金額）
- 進入第一輪時自動合併手機提交與老師手動輸入的價格
- 老師仍可在網頁上手動輸入價格，兩種方式並行不衝突
- server.js 新增 WTP API 端點（提交、查詢、清除、開關控制）

### ver.1.5.0 — 2026-04-03
**QR Code 手機掃碼定價功能**
- 新增 `server.js`：Node.js 本地伺服器，提供 API 端點供手機提交定價
- 新增 `mobile.html`：手機端定價頁面，含四步驟流程：
  1. 選擇組別（已送出的組別會顯示為不可選）
  2. 輸入定價金額
  3. 確認定價內容（可返回修改）
  4. 送出定價（送出後鎖定，不可再修改）
- 各輪定價階段自動顯示 QR Code，各組代表掃碼後可在手機上輸入定價
- 主畫面即時輪詢伺服器，已送出的組別自動填入價格並標示狀態
- 第三/四輪改價對話框中也支援 QR Code 掃碼輸入
- 向下相容：若未啟動 server.js，自動降級為手動輸入模式（QR 不顯示）
- 使用方式：`node server.js` 啟動伺服器，瀏覽器開啟 `http://localhost:3000`

### ver.1.4.0 — 2026-04-03
**新增隨機定價按鈕（QC 測試用）**
- 各輪定價區新增「隨機填入定價（QC 測試用）」按鈕，一鍵為所有組別填入隨機價格
- 第三、四輪的改價對話框中也提供同樣的隨機按鈕
- 價格範圍：最低願付價格 × 0.9 ~ 最高願付價格 × 1.1
- 用於開發階段快速測試遊戲流程

### ver.1.3.0 — 2026-04-03
**新增版本號與即時時鐘**
- Header 右上角顯示版本號與更新日期
- 顯示即時時鐘（yyyy/mm/dd hh:mm:ss），每秒自動更新
- Header 改為 flexbox 佈局，標題靠左、版本與時鐘靠右

### ver.1.2.0 — 2026-04-03
**千分位數、改價暫停按鈕、全寬版面**
- 所有金額加上千分位逗號分隔符（例如 $1,234）
- 第三/四輪每抽完 N 位顧客後，改為先顯示績效摘要看板，學生可檢視目前表現後再點擊「開始重新定價」按鈕進入改價對話框（原為自動彈出）
- 移除頁面 max-width 限制，改為全寬顯示，確保 8 組資料在同一行完整呈現
- 表格使用 table-layout:fixed 均分欄寬
- 改價對話框加寬至 900px

### ver.1.1.0 — 2026-04-03
**繁體中文介面與手動/自動抽籤模式**
- 全部介面文字轉換為繁體中文（導航、設定、遊戲過程、結果排名、頒獎台等）
- 設定頁新增「抽籤模式」選項：
  - 電腦自動抽籤：從願付價格池中隨機抽取（原有功能）
  - 手動輸入：老師從紙箱抽出便利貼後將價格輸入系統

### ver.1.0.0 — 2026-04-03
**初始版本 — Excel 轉 Web**
- 將 Google Spreadsheet 版 Pricing Game 轉換為網頁版
- 可調整參數：學生人數、分組數、每輪顧客數、改價間隔、庫存上限、產品名稱
- WTP（顧客願付價格）收集：逐一輸入、批次貼上、隨機產生
- 四輪遊戲：
  - 第一輪：各組固定定價，隨機抽取顧客比對成交
  - 第二輪：參考市場結果後重新定價
  - 第三輪：每 N 位顧客可重新定價（動態定價）
  - 第四輪（決勝輪）：同第三輪 + 庫存限制，售完即止
- 每輪結束顯示成交數、成交率、營收、平均成交價排名
- 第二輪起顯示累計排名
- 最終結果頁含前三名頒獎台與四輪營收明細
- 響應式設計，適合教室投影使用
- 純前端實作（HTML + CSS + JS），無需伺服器
